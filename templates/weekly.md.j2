# Weekly Digest - {{ week_start }} to {{ week_end }}

## Podcasts ({{ podcasts|length }})
{% if podcasts -%}
{% for podcast in podcasts -%}
- **{{ podcast.title }}** ({{ podcast.rating_llm }}/5) — {{ podcast.description }}
  - [View note]({{ podcast.note_link }})
{% endfor %}
{% else -%}
- _No podcasts processed._
{% endif %}

## Newsletters ({{ newsletters|length }})
{% if newsletters -%}
{% for newsletter in newsletters -%}
- **{{ newsletter.title }}** — {{ newsletter.description }}
  - [Open in Gmail]({{ newsletter.source_link }})
{% endfor %}
{% else -%}
- _No newsletters processed._
{% endif %}

{% if failures -%}
## ⚠️ Failures ({{ failures|length }})
{% for failure in failures -%}
- **{{ failure.title }}** ({{ failure.type }}) — {{ failure.error_reason }}
{% endfor %}
{% endif %}
